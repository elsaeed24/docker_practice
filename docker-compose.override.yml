version: "3"
services:
  node-app:
    build:
      context: .
      target: development
    volumes:
      - ./Src:/app/Src:ro
    environment:
      - PORT=4000
      - NODE_ENV=development
    command: npm run start-dev
